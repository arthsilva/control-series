<!doctype html>
<html lang="pt-BR">
    <head>
        <meta charset="UTF-8">
        <title>{{ $title }} - Controle de Séries</title>
    </head>
    <body>
        <h1>{{ $title }}</h1>
        {{ $slot }}
    </body>
</html>